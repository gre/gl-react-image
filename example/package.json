{
  "name": "example",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "gl-react": "*",
    "gl-react-dom": "*",
    "gl-react-image": "file:..",
    "react": "*",
    "react-dom": "*"
  },
  "devDependencies": {
    "react-scripts": "1.0.12"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy":
      "react-scripts build && surge -p build -d https://gl-react-image.surge.sh"
  }
}
